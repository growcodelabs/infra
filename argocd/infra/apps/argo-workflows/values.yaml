crds:
  upgradeJob:
    image:
      tag: v1.32.0

server:
  authModes:
    - server

workflow:
  serviceAccount:
    create: true
  rbac:
    create: true

controller:
  workflowNamespaces:
    - argo
